# summary — vdp

Cross-study synthesis of the VDP sweeps (`../log_penalty`,
`../frac_exp_penalty`, `../baseline`) into shared frontier figures and tables.
Findings are in `results.md` (generated by `analysis.py`).
