# papar/ — working paper (Master thesis)

LaTeX sources for the thesis (`Mthesis.tex`, `ref.bib`); build artifacts are
gitignored. Figures in `plot/` are copies of curated experiment figures —
refresh them with `make paper-figures` (the mapping is by basename against
`experiments/*/figures/`), do not edit them by hand.

Note: `docs/research/OVERVIEW.md` (theory program) targets a separate thesis
copy at `~/Documents/Repos/tex-files/Thesis/Mthesis.tex`; the two copies may
have diverged and have not been reconciled.
