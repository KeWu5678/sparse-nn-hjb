# @package _global_
# Pendulum switching-band oversampling variant: 6k + 2k near S (Table 4, tab:rs_oversampling).
# Same switching-set structure as conf/data/pendulum.yaml, so it opts into the same
# region_split eval and the same shared out-of-sample pool; only the training set and
# its dataset-aligned distance cache differ.
defaults:
  - /eval: region_split
data:
  path: Pendulum_2sided_oversample_20260704/add2k.npz
eval:
  distance_cache: Pendulum_2sided_oversample_20260704/add2k_region_distances.npz
